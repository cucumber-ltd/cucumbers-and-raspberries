{
  "scripts": {
    "mocha": "mocha",
    "unit": "mocha lib/*.spec.js lib/**/*.spec.js --grep @slow --invert",
    "unit-slow": "mocha lib/*.spec.js lib/**/*.spec.js --grep @slow",
    "unit-all": "npm run unit && npm run unit-slow",
    "acceptance": "cucumber-js",
    "acceptance-slow": "player_type=mpc_player ui_type=gpio_ui cucumber-js",
    "acceptance-all": "npm run acceptance && npm run acceptance-slow",
    "test": "npm run unit && npm run acceptance",
    "test-all": "npm run unit-all && npm run acceptance-all",
    "start": "node index.js"
  },
  "dependencies": {
    "onoff": "^3.0.2"
  },
  "devDependencies": {
    "cucumber": "^4.2.1",
    "mocha": "^5.1.1",
    "read": "^1.0.7",
    "sinon": "^5.0.6"
  }
}
